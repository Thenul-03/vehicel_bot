{
  "builds": [
    { "src": "server.py", "use": "@vercel/python" },
    { "src": "UI/package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "/assets/(.*)", "dest": "/UI/dist/assets/$1" },
    { "src": "/(.*)", "dest": "server.py" }
  ]
}